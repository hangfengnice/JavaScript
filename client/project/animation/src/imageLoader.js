

function loadImage (images, callback, timeout) {
  let count = 0
  let success = true
  let timeoutId = 0
  let isTimeout = false

  for(var key in images) {
    // if 
  }
}

export {
  loadImage
}
