class CircularQueue {
  items = null
  n = 0
  head = 0
  tail = 0

  constructor(capacity) {
    this.items = Array(capacity)
    this.n = capacity
  }
  enqueue(item) {
    console.log(tail)
    if(tail) {

    }
  }
}

let c = new CircularQueue(8)

c.enqueue()